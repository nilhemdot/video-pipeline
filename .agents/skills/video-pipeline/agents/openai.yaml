interface:
  display_name: "Video Pipeline"
  short_description: "Repo-specific patterns and workflows for video-pipeline"
  default_prompt: "Use the video-pipeline repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true